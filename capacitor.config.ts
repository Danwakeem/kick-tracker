import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'baby-kick-counter',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
